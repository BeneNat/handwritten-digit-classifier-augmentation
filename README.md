# handwritten-digit-classifier-augmentation
The project's primary goal is to develop a convolutional neural network (CNN) for recognizing handwritten digits (MNIST) and to analyze the impact of data augmentation on its accuracy and generalization.
